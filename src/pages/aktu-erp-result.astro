---
// 301 Permanent Redirect to Home
const url = new URL(Astro.request.url);
const roll = url.searchParams.get('roll') || url.searchParams.get('rollNumber');
const target = roll ? `/?roll=${encodeURIComponent(roll)}` : '/';
return Astro.redirect(target, 301);
---
